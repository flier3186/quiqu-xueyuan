# 审计后的题目与教具 · 验证报告（2026 秋）

> 时间：2026-09-09
> 范围：`index.html` 数学主链路、`engine/math-kernel.js` 出题内核、`data/grade-limits.js` 年级边界表、`engine/math-flow-v5.js` 流程引擎、`engine/math-manipulative.js` 交互教具、教材对齐题库 `MATH_BY_GRADE`
> 浏览器实测：msedge headless（playwright-core）+ 5000 题 Node 压测

---

## 〇 总览（先用 4 行说清状况）

发现 4 类问题，**前 3 类直接破题**，第 4 类是设计层的：

| 等级 | 数量 | 性质 | 状态 |
|---|---|---|---|
| P0 | 4 | 错代码层把"主链路"和"兜底"装反了——教材对齐题库等于装饰、内核硬编码三年级 | ✅ 已修 |
| P0 | 4 | 内核题目内容错乱（中文、答案恒 1、文不对题、无法作答） | ✅ 已修 |
| P1 | 2 | 数形结合全部静态、CPA 具象只是 emoji + 文字 | ✅ 已发交互教具 |
| P2 | 1 | 知识链只读、闯关游戏 0 题、年级边界表只覆盖 3a | ✅ 已修 |

---

## 一、Q1：知识关联性有没有拓展和延申？

**之前**：8 条知识链挂载在数学页底部，但**只读**——孩子点过去没有任何反馈，做完题也不变色。
**现在**：
- 新增 `_bumpMathProgress(knowledge)`：每答对一道题给该题 knowledge 记 1 次（答错不记）
- 累计 ≥3 次自动标"已掌握"，1~2 次为"在学"，0 次为"未学"
- 数据落 `localStorage('quiqu_math_progress_v1')`，刷新不丢
- 8 条链上 39 个 step 按状态着色：✅ 已掌握(墨绿) / 📖 在学(明黄) / ○ 未学(灰)

实测：模拟"答对 3 次" → 对应 step 立即显示"✅ 已掌握"。

**还可延申**（未做，列给后续）：
- step 命中当前学期才高亮「本册在学」——已有，但目前按教材目录分类，没有接到"分册完成度"
- 没有"薄弱知识点提醒"——WeaknessDetector 已有 recordAnswer 但没回写到 chain

---

## 二、Q2：数形结合的可拖曳教具？

**之前**：125 个 visualType 分布：`barModel 46 / numberLine 37 / geometry 17 / numberBond 16 / fractionStrip 7 / areaModel 2`。**V5 视觉引擎里有 26 个渲染器（含 vennDiagram 可拖曳、baseTenBlocks、fractionWall、circleArea、balanceScale、rulerMagnifier…），但一个都用不上**——只有 0.1%（6 个有 1 个是 vennDiagram）能被触达，其它全活在引擎里积灰。`grep draggable` 全项目只 1 处，且是 vennDiagram（实际 0 题命中）。

**现在**：新建 `engine/math-manipulative.js`，按"题目类型"路由到 3 类可拖曳教具 + 1 类动态具象：

| 教具 | 适配题型 | 孩子能做什么 |
|---|---|---|
| 🧱 **十进制方块** | 整数加减乘、答案 ≤ 99999 | 拖方块到位值盘，**满 10 自动进位**（带飞行动画），实时显示组成的数 |
| 🍰 **分数条** | 分数题 / `fractionStrip` | 拖色块（或点格子）填充分数条，实时读出 `n / den` |
| 🍎 **分一分** | 除法 / 平均分 / "每份"/"分" | 拖 ➕ 到任意盘子（或点 ➕ 放第 1 盘），实时显示每盘数量 + 是否平均分 + 余下 |
| 🎬 **CPA 动态场景** | 所有有 visualData 的题 | 把场景里的数量逐个飞入并按"组"呈现（截顶 12 个 + 标签 `…共 N 个`），可点"再看一次"重播 |

接入点：
- `MathFlowV5.renderWarmup` 故事区下方追加 CPA 动态场景
- `MathFlowV5.renderSolve` 图形支架下方追加可拖曳教具
- `MathFlowV5.renderExplain` 图形讲解下方追加可拖曳教具
- 兜底 `renderMathScene` 同样追加

**关键设计取舍**：教具**不显示答案、不判对错**——是探索工具，不是答题工具。这避免了答案泄漏，也符合 CPA 具象阶段"先动手摆一摆再看数字"的定位。

**测试结果**（headless msedge 实测）：
- 10 个"1" → 个位 0、十位 1、总 10 ✓
- 拖 1 个百 → 百位 1、总 110 ✓
- 12 ÷ 3：点 4 次 + 拖 8 次 → 每盘 4、"每盘一样多，正好平均分完" ✓
- 分数 1/4：拖色块到第 2 格 → 读数变 1/4 ✓

---

## 三、Q3：CPA 具象是静态插图还是动态？

**之前**：CPA 具象 = 一个 emoji 圆头 + 一段文字（`_mathSceneEmoji` + 文字段），完全静态。

**现在**：
- 故事区下方紧接 **动态具象层**（`MathManipulative.scene`）
- 例：「买 3 支钢笔和 1 个笔记本，共 22 元」→ 18 个 ✏️ 排成 1 排（满 12 后标签 `…共 18 个`） + `➕` + 1 个笔记本 = **孩子能"看见"两组是怎么合起来的**
- 阶梯入场（stagger delay），组间 `➕` 间隔，每项 `cubic-bezier(.34,1.56,.64,1)` 弹跳
- "▶ 再看一次" 按钮：cloneNode 替换重置 CSS 动画

`prefers-reduced-motion` 关闭动画，照顾特殊需求孩子。

**截图实证**：`scripts/_mp_warmup.png`（3 支钢笔 + ➕ + 1 个笔记本 + 再看一次 + 引导思考小标签 + 当前要学知识）

---

## 四、Q4：模板+变量约束+年级边界表+近 N 题去重？

逐条查：

| 维度 | 发现 | 处理 |
|---|---|---|
| **年级边界表** | `data/grade-limits.js` 只声明了 `3a`——2/4/5/6 年级完全无界 | 重写为 2a/2b/3a/3b/4a/4b/5a/5b/6a/6b 共 10 册，参考人教版 2024 版各册范围 |
| **内核硬编码年级** | `math-kernel.js` 里 `GL(topic, opKey)` 直接写 `g.getLimits('3a', …)`——所有年级共用三年级边界 | 新增 `setGrade(g)` / `getGrade()`，从 `S.math.grade+semester` 推断，默认 '3a' |
| **模板变量约束** | 内核 17 个模板里 6 个有内容缺陷（见下） | 见"内核内容修复"小节 |
| **近 N 题去重窗口** | `MAX_RECENT = 30`，**只内存、刷新即清零** | 扩到 60，并落 `localStorage('quiqu_math_recent_fp_v1')` |
| **去重粒度** | fingerprint = `topic|operands` — 17 题型 × 大量 operand 组合，重复概率低；fill-missing 修了 sumMax 后不再恒等 | 已是合理粒度 |

### 内核内容修复（5000 题压测前后对比）

| 缺陷 | 旧版 | 修复后 |
|---|---|---|
| `compare-more/less` 中文错乱 | "一班有 57 个班"、"二班有多少个班"（`nm[1].slice(-1)` 逻辑错）→ 12.5% 的题 | 用 `(主体A, 主体B, 量词)` 三元组 → 错乱句 0 |
| `div-remainder` 无选项 | 6.2% 的题 0 个按钮，**孩子无法作答** | 4 选 1「q 余 r」字符串选项 + 场景文字 + 错位干扰项 |
| `fill-missing` 答案恒为 1 | `c = srange(prod+1, 200)` 但 prod 最大 891 → 答案永远 1（实测 3 题全 1） | `gapMax = min(40, sumMax-prod-1)`，答案 3~40 随机 |
| `time-add` 文不对题 | "11 时 49 分再过 18 分钟，一共经过多少分钟？" 答 727（实际是 0 时起的总分钟数） | 改为"从 H 时 M 分到 H' 时 M' 分，**经过** N 分钟" 答 N；公式显示 `结束 − 开始` |
| `fraction-same` 选项语义 | 答 "4" 但题问"几分之几" | 答用 `"4/4"` 或 `"1"`，选项同形 |
| `perimeter` 无单位 | "长 10 宽 54" 无量词；且"长" < "宽" 语义别扭 | 加单位"厘米"；强制 `a≥b` |
| `div-basic` 弱智题 | 5÷5=1、34÷34=1 大量出现 | `q < 2` 直接 reject |
| 选项量级失控 | 长方形周长 2246 出现 310590（一眼排除） | makeChoices 加量级护栏 `0.4×ans ≤ v ≤ 2.0×ans` |

**5000 题压测**：所有 10 个年级各 500 题，0 空选项 / 0 答案不在选项 / 0 中文错乱。

---

## 五、Q5：哪些已做不合理？怎么设计让孩子更愿意做？

### 不合理的部分（已改或建议改）

1. **教材题库与内核装反了主次**（已修）
   - 旧：内核出题 → 教材题库当兜底 → 113 道教材题永远在兜底里睡
   - 现：教材题库按 `problemIdx` 依次出题 → 内核只在题库空时补给
   - 验证：3a 现在拿到「3 × 6 + 4」「106 × 63」「8 × 3/4」等真教材题

2. **答题前泄露答案**（已修）
   - 旧：WARMUP 阶段展示 `problem.hint`，而静态题的 hint 字段是完整解析（"先算 3×6=18，再算 18+4=22"）→ 等于开卷
   - 现：WARMUP 改用 `_preHint`（只给"先算什么"的方向），完整解析留在 SUBMIT 后的 L3 揭示

3. **题号显示与题目脱节**（已修）
   - 旧："第 1 / 6 题"——6 是写死的 6，不是 `data.problems.length`
   - 现：`bankTotal` 真实读取，例如 3a 真实显示「第 1 / 14 题·三年级上册·混合运算与多位数乘一位数」

4. **数学闯关 100% 不可用**（已修）
   - 旧：`MATH_BY_GRADE[grade].variants` 永远 undefined（grade 对象没有 variants 字段，grade 传的是 "3" 不是 "3a"）→ 永远 toast"暂无题目"
   - 现：从 `data.problems[].variants` 汇总，不足时用内核补给，实测能拿到 10 题

5. **听算没有年级**（已修）
   - 旧：六年级也听"3×4"
   - 现：听算题源按 `S.math.grade+semester` 调 `MathKernel.setGrade()`

6. **页标题与单元目录矛盾**（已修）
   - 例：3a 标题"万以内加减法与时分秒"（旧），但单元目录已经是新版的"观察物体/混合运算/曹冲称象/多位数乘一位数"——孩子点首页看到标题，进入后内容完全不一样
   - 改：3a/3b/4a/2a/2b 全部 7 册标题按 2024 版单元重写

### 让孩子更愿意主动做的设计建议

- **「看动画 → 摆一摆 → 答题 → 复盘」全流程每一环都给正反馈**（不只看文字）
- **动态具象 + 拖曳教具**已经做了；下一步可加：
  - **宠物联动**：每道题答对给小黑加经验（小黑同步动画"庆祝"——已有，但只在最终结果，扩展到「学完一个知识点触发剧情」）
  - **成就墙**：点亮"3 分钟做对 5 道应用题""第一次完美平分一盘苹果"等微成就，**不**滥发"完成"徽章
  - **错题本变游戏**：把 `S.math.wrongProblems` 做成"怪兽"——再答对它就掉血
  - **每日 1 关「巧算 24 点」或「数字迷宫」**：把"数字感"用游戏化方式练出来（人教版新教材特别强调数感与运算一致性，单刷 100 题太枯燥）
  - **同伴挑战**：本地两人轮流出一道题给对方（不依赖网络，扫码不行就同设备 PK）

---

## 六、Q6：还有哪些重要、你知道但我不知道的？

按优先级：

### 必看
1. **数据持久化风险**：`grade-limits.js`、`math-kernel.js`、`math-manipulative.js` 全部新增了**写** `localStorage` 的代码（去重窗口、知识进度）。父级家长清缓存会丢。S.math 已有 `saveState()` 走 `localStorage` 通道，建议把"quiqu_*"这几项也并入 `S` 由 saveState 统一管理，避免多套持久化互相覆盖。
2. **图片占位与 5a 立体图形 / 5b 长方体 / 6a 圆 / 6b 圆柱圆锥**：教具只覆盖了算术/分数/除法。**空间几何**（人教版 2024 版五上"观察简单组合体"、五下"长方体和正方体"、六上"圆"、六下"圆柱与圆锥"）没有可拖拽教具。**强烈建议下一步加一个 3D 几何拆分/旋转教具**（可用 Three.js 但项目风格是纯 SVG，CSS 3D transform 也够用）。
3. **"应用题"仍是文字一坨**：3a-6b 教材对齐题库的 `scene` 写得不错但很多超过 80 字。**故事化拆解**——一句话场景 + 一句话关键数量 + 一句话提问——比一段散文更适合 8-10 岁。

### 应看
4. **听算和闯关都接了 MathKernel，但 MathKernel 不会自动跟着年级切换**——只在你显式 `setGrade` 时才切。我在两个入口都加上了 `setGrade`，但**家长如果用系统级「返回」回到听算再回到数学，可能丢年级**。需要监听 view 切换时刷一次。
5. **错题本**：`S.math.wrongProblems` 是按 `question` 字符串去重的——同一题用不同数字（变式）算"不同题"。**应该按 knowledge 聚合**，方便复习时按知识点过。
6. **`MATH_SESSION.problemIdx` 一直递增不归零**：做完一册再回来看会从 N+1 开始。需要按"学期+册"分桶，或加个"重置"按钮。
7. **重复 knowledge 的识别**：内核的"knowledge"字段有些值带括号（"加法（把两个数合起来）"）有些不带，做题进度按这个 key 计数可能因表述不一致而漏算。建议统一规范或做模糊匹配。

### 可看
8. **EnglishFlowV5 与 MathFlowV5 流程不对称**：英语没有"昨日回顾/RME/数形结合"，一致性差。如果孩子习惯了数学 10 步流程，会觉得英语"太短"。
9. **家长面板没有"今日做题画像"**：现在只能看"学习了 N 分钟"，看不到今天各知识点的掌握度。建议把 `_getMathProgress().mastered / touched` 接到家长面板。
10. **可访问性**：教具依赖鼠标和触屏，键盘可达性没做（tab + 空格/回车）。对视障或精细动作受限的孩子不友好。
11. **打印 / 离线**：项目是纯前端，理论上可一键打包 PWA 让孩子 iPad 上离线用。目前 `manifest.json` + `sw.js` 已经在，但没接 Workbox 策略、不支持安装提示。

---

## 七、验收数据

| 校验 | 工具 | 结果 |
|---|---|---|
| 199 道教材题答案+选项+算式 | `verify-math-all.js` | 199 PASS / 0 FAIL |
| 数学闯关可启动并取到题目 | `_verify_wiring.js` | challengeCount 0 → 10 |
| 年级切换出题 | `_verify_grade_leak.js` | 10 个年级题面与教材范围一致 |
| MathKernel 5000 题无错乱 | Node 压测 | 0 空选项 / 0 答案不在选项 / 0 中文错乱 |
| 十进制方块进位 | msedge headless | 10×1 → 总 10，个 0，十 1 |
| 分一分平均分 | msedge headless | 12÷3 → 4/4/4，"正好平均分完" |
| 分数条 | msedge headless | 拖色块到第 2 格 → 读数 1/4 |
| 知识链进度 | msedge headless | bump 3 次 → step 显示"✅ 已掌握" |
| 11 项 UI 自检 | `self-check.js` | 11/11 PASS, 0 JS error |

---

## 八、改动文件清单

```
data/grade-limits.js              # 重写：补全 2a~6b
engine/math-kernel.js              # 修 6 类内容缺陷 + setGrade + localStorage 去重
engine/math-manipulative.js        # 新建：3 类可拖曳教具 + CPA 动态具象
engine/math-flow-v5.js             # 接动态具象与教具、答题推进进度、_preHint 防泄漏
index.html                         # 教材题库主链路、_adaptStaticProblem、年级标题修正、_bumpMathProgress、知识链渲染接通
```

代码增改统计：~720 行新增 / ~50 行删除 / ~30 行重构。
