# AGENTS.md · 奇趣学园 项目规则

> 本文件定义所有 AI Agent（包括 TRAE、Codex、Claude 等）在本项目中工作时的规则与约束。
> 修改本文件等同于修改项目宪法，请谨慎操作。

---

## 1. 项目概述

- **项目名称**：奇趣学园（quiqu-xueyuan）
- **目标用户**：8-12 岁小学生（当前重点：二年级升三年级）
- **核心定位**：提升数学与英语「能力」的趣味学习工具，非应试刷题
- **技术形态**：纯前端静态站点（HTML + CSS + JS，无后端）
- **部署平台**：Cloudflare Pages（主） + GitHub Pages（备）
- **仓库地址**：https://github.com/flier3186/quiqu-xueyuan

---

## 2. 编程行为准则

### 〇、用户指令第一原则（绝对服从）

**用户说的每一句话，100%都是重要的。** Agent必须严格、完整、逐条执行用户给出的**所有**指令，严禁自行判断哪些"重要"、哪些"不重要"、哪些"可以不做"。

- 用户的每一条指令（无论是写在同一条消息里的多条，还是分多条消息发的）都必须被完整理解并全部执行，**不允许挑着执行**
- 严禁以"我认为不需要"、"这是细节"、"和主要目标无关"等任何理由**跳过、忽略或打折扣执行**用户的任何一条指令
- 如果同一条指令前后有矛盾，以用户**最新**的表述为准，并指出矛盾让用户确认；**不能静默选一种**
- 执行完后必须逐条向用户**核对已完成的指令**，做到"用户说了什么、我做了什么"一目了然
- 如果有指令不清楚或不确定如何做，**先问再做**，不能不做也不能猜

**违反本条的 AI Agent 回复视为不合格。**

### 一、先想再写

不要假设，不要隐藏困惑，主动暴露权衡。

- 把你的假设明确说出来，不确定就问，不要猜
- 存在歧义时，呈现多种解读，不要默默选一种
- 如果还有更简单的方案，直接说
- 感到困惑时停下来，说出哪里不清楚，主动请求澄清

### 二、极简至上

用最少代码解决问题，不写任何推测性代码。

- 不被要求的功能一律不写
- 仅为一次使用不建抽象层
- 不被要求的"灵活性"和"可配置性"一律不加
- 不给不可能发生的场景写错误处理
- 优先用最少代码解决问题，但不牺牲可读性

核心检验：资深工程师会不会说"过度设计"？会 → 简化；会不会说"难懂"？会 → 适当展开。

### 三、精准手术

只碰你必须改的，只清理你自己的垃圾。

编辑已有代码时：
- 不要"顺手优化"相邻的代码、注释或格式
- 不要重构没坏的东西
- 匹配已有代码风格，哪怕你觉得你的写法更好
- 如果你发现无关的死代码，提一句就好——不要删

你的修改产生了死代码时：
- 清理因为你修改而变成废弃的 import / 变量 / 函数
- 修改前就存在的死代码：提一句即可，由用户决定是否删；用户明确要求删除时才删

核心检验：每一个被改动的代码行，都应当能追溯到用户的原始需求。

### 四、目标驱动

先定义成功标准，然后循环执行直到达标。

把命令式指令转化为可验证目标：
- 不要这样写：加个校验
- 改成这样：先写给无效输入的测试，让测试失败，再写代码让它通过
- 不要这样写：修一下这个 bug
- 改成这样：先写一个能复现 bug 的测试，确认失败，再修代码让它通过
- 不要这样写：重构一下 X
- 改成这样：确保重构前后所有已有测试依然通过

多步骤任务，每一步都必须写明验证方法：
1. [步骤] → 验证: [检查项]
2. [步骤] → 验证: [检查项]
3. [步骤] → 验证: [检查项]

目标定得好，Agent 能独立循环。目标含糊（"把它搞对"），就得反复确认。

### 五、角色边界（单 Agent 项目适用）

本项目为纯前端静态站点，主 Agent 同时承担规划与执行。

- UI 设计变更需先出方案并附视觉描述，用户确认后再写代码
- 教学内容结构（知识点 / 题库 / 场景库）变更需先说明影响范围
- 推荐与评估算法（LearningPath / WeaknessDetector）修改需先说明算法改动点
- 所有需用户确认的节点，必须先输出方案再继续，禁止静默跳过

### 六、两个确认卡口

- **方案确认**：产出执行方案后，必须先输出完整方案并等待用户确认，禁止静默跳过直接执行代码改动。即使用户说"继续"，也必须确认是"确认方案并执行"还是"继续讨论方案"。
- **阶段确认**：每个里程碑（如数学板块完成、口语板块完成）输出演示证据后等用户确认再进入下一阶段。

### 七、上下文管理

- 对话中上下文使用率超过 80% 时，主动告知用户并建议点击对话底部的"压缩"按钮压缩上下文，避免约束被遗忘；压缩后重新声明关键约束
- 单个任务对话轮次超过 20 轮且涉及多模块时，建议用户开新任务继续，旧任务的关键结论由主 Agent 写入阶段报告

### 八、文件引用

- 用户涉及任何文件时，必须用 # 符号引用或拖拽文件入框，禁止手工复制粘贴代码片段
- AI 收到手抄代码片段时，应提示用户用 # 引用原文件以获取完整上下文

### 九、工作流选择

- 边界清晰的单次任务（如修一个 Bug、加一个字段）：直接对话执行
- 中小型功能开发、模块重构：用 /plan 命令启动 Plan 工作流，先确认计划再执行
- 系统级复杂任务、多人协作模块：用 /spec 命令启动 Spec 工作流，生成 spec/tasks/checklist 三件套

### 十、需求变更评估

- 收到需求变更时，主 Agent 必须先评估影响范围和返工成本，输出评估表后再动手改代码
- 评估表格式：受影响模块 | 受影响文件 | 返工预估 | 是否影响已完成里程碑
- 用户确认评估表后才开始执行变更

### 十一、验证必须附证据

- 每次声称"已完成"或"已验证"时，必须附带验证证据，禁止只说结论不提供依据
- 验证分为 4 个层级，必须明确标注验证到哪一层：
  - L1 JS 语法校验通过（node --check / new Function）
  - L2 本地浏览器运行通过（启动 http.server 实测）
  - L3 多端验证通过（桌面 + 手机端打开线上地址实测）
  - L4 教学逻辑验证（数形结合是否出图、语音是否有声、答题链路是否闭环）
- 以下行为严格禁止：
  - 工具加载失败时声称"已验证"
  - 仅凭"本地测试通过"声称线上已生效
  - 将语法校验等同于真机运行
  - 原生 API 改动不经过用户确认就声称通过
- 暂存区状态必须检查：git add ≠ git commit，commit 前查 status，push 前查 log

### 十二、修改前必做

- [ ] 先 Read 目标文件，理解现有结构再动手
- [ ] 不破坏现有功能，只新增或局部修改
- [ ] 不创建非必要文件，优先编辑现有文件
- [ ] 遵守现有命名规范与代码风格

### 十三、禁止行为

- ❌ 不修改 defaultState 的现有字段（只能新增）
- ❌ 不删除已部署的引擎对象（MathVisual / LearningPath / WeaknessDetector / ProgressTracker / TimeController / SpeakEngine）
- ❌ 不引入后端依赖（保持纯前端可静态部署）
- ❌ 不使用需要 API Key 的付费服务（用户未配置）
- ❌ 不创建 README / 文档文件（除非用户明确要求）

### 十四、提交规范

- commit message 格式：`<type>: <描述>`
- type 可选：feat / fix / refactor / docs / style
- 描述用中文，简洁明了

---

## 3. 本项目特有红线（踩坑总结）

- SVG 动画必须加 `-webkit-transform-box: fill-box` 前缀，否则 iOS / 安卓不生效
- 语音播放必须用户手势触发解锁，不能自动播放（移动端限制）
- 部署后必须实测线上地址，不能只看本地（曾出现"本地通过但线上没更新"）
- 数形结合是教学铁律，任何数学题改动都必须保留可视化
- localStorage key 固定为 `ququxv4_state_v1`，改结构只能加字段不能删字段

---

## 4. 教学设计铁律

### 4.1 数学板块
- **数形结合是核心规则**：每道题必须有可视化图形
- 可视化类型：barModel / areaModel / numberBond / fractionStrip / numberLine / geometry
- 调用 `MathVisual.render(type, data)` 渲染，不要手写 SVG
- 答案讲解阶段必须配图，不能只有文字
- 教学流程遵循 5 阶段：场景导入 → 引导发现 → 正式解题 → 数形结合讲解 → 发散思维

### 4.2 英语板块
- 自然拼读不用音标（IPA）
- 单词释义必须含 5 要素：词性、释义、例句、搭配、同反义词
- 发音用 Web Speech API，不依赖外部 TTS 服务

### 4.3 口语板块
- 对话基于 SPEAK_SCENARIOS 场景库
- 语音识别用 Web Speech API SpeechRecognition
- 发音评分用 Levenshtein 距离算法

### 4.4 内容标准
- 所有内容用 8-12 岁能理解的语言
- 题目场景化（生活故事），不直接出题
- 难度自适应，按年级分级

---

## 5. 技术约束

### 5.1 文件结构
```
prototype/v4/
├── index.html          # 单文件应用（所有代码）
├── assets/             # 图片资源
├── vercel.json         # Vercel 配置（备用）
├── .github/workflows/  # GitHub Actions
└── AGENTS.md           # 本文件
```

### 5.2 数据持久化
- 使用 localStorage（key: `ququxv4_state_v1`）
- state 结构见 index.html 中的 `defaultState`
- 保存调用 `saveState()`，读取调用 `loadState()`

### 5.3 配色规范
- 主色：孔雀绿 `--teal: #00A896`
- 辅色：玉子黄 `--yellow: #F5B800`
- 强调：珊瑚 `--coral: #FB923C` / 粉 `--pink: #E8A0BF` / 深蓝 `--navy: #1E3A5F`
- 必须使用 CSS 变量，不要硬编码颜色

### 5.4 响应式断点
- 桌面：> 980px
- 平板：641-980px
- 手机：≤ 640px
- 超小屏：≤ 380px

### 5.5 浏览器兼容
- SVG 动画必须加 `-webkit-transform-box: fill-box` 前缀
- 语音播放需用户手势触发（移动端限制），不自动播放
- SpeechRecognition 不支持时降级为文本输入

---

## 6. 部署流程

### 6.1 Cloudflare Pages（主）
```bash
CLOUDFLARE_API_TOKEN=xxx CLOUDFLARE_ACCOUNT_ID=xxx \
npx wrangler pages deploy . --project-name=quiqu-xueyuan --branch=main
```

### 6.2 GitHub Pages（备）
- push 到 master 分支 → GitHub Actions 自动部署
- 访问：https://flier3186.github.io/quiqu-xueyuan/

### 6.3 访问地址
| 平台 | 地址 | 用途 |
|---|---|---|
| Cloudflare Pages | https://quiqu-xueyuan.pages.dev | 国内主入口 |
| GitHub Pages | https://flier3186.github.io/quiqu-xueyuan/ | 备用 |

---

## 7. 待办与已知限制

- [ ] 口语 AI 基于场景库关键词匹配，非真 LLM（静态站点限制）
- [ ] 数学题库 48 道，刷多次会重复，待扩充
- [ ] 周报数据需学习几天后才有意义
- [ ] （在此追加你的待办）
